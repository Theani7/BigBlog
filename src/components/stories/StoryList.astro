---
interface Story {
  title: string;
  slug?: string;
  excerpt?: string;
  meta1: string; // e.g. "Last edited X days ago" or "Published Jul 28"
  meta2?: string; // e.g. word count or read time
}

interface Props {
  stories: Story[];
  emptyText?: string;
}

const { stories, emptyText = 'No stories yet' } = Astro.props;
---

{
  stories.length > 0 ? (
    <div class="story-list">
      {stories.map((story) => (
        <article class="story-card">
          <div class="story-content">
            <h2 class="story-title">
              <a href={story.slug ? `/p/${story.slug}` : '#'}>{story.title}</a>
            </h2>
            {story.excerpt && <p class="story-excerpt">{story.excerpt}</p>}
            <div class="story-meta">
              <span class="meta-item timestamp">{story.meta1}</span>
              {story.meta2 && (
                <>
                  <span class="meta-divider">·</span>
                  <span class="meta-item badge">{story.meta2}</span>
                </>
              )}
            </div>
          </div>

          <div class="story-actions">
            <button class="action-btn" title="Edit">
              <svg
                width="18"
                height="18"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
                stroke-linecap="round"
                stroke-linejoin="round"
              >
                <path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7" />
                <path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z" />
              </svg>
            </button>
            <button class="action-btn" title="Publish">
              <svg
                width="18"
                height="18"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
                stroke-linecap="round"
                stroke-linejoin="round"
              >
                <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14" />
                <polyline points="22 4 12 14.01 9 11.01" />
              </svg>
            </button>
            <button class="action-btn delete" title="Delete">
              <svg
                width="18"
                height="18"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
                stroke-linecap="round"
                stroke-linejoin="round"
              >
                <polyline points="3 6 5 6 21 6" />
                <path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2" />
              </svg>
            </button>
            <button class="story-options" aria-label="Story options">
              <svg
                width="20"
                height="20"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
                stroke-linecap="round"
                stroke-linejoin="round"
              >
                <polyline points="6 9 12 15 18 9" />
              </svg>
            </button>
          </div>
        </article>
      ))}
    </div>
  ) : (
    <div class="stories-empty-state">
      <div class="empty-icon">
        <svg
          width="48"
          height="48"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="1.5"
          stroke-linecap="round"
          stroke-linejoin="round"
        >
          <path d="M14.5 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V7.5L14.5 2z" />
          <polyline points="14 2 14 8 20 8" />
          <line x1="16" y1="13" x2="8" y2="13" />
          <line x1="16" y1="17" x2="8" y2="17" />
          <line x1="10" y1="9" x2="8" y2="9" />
        </svg>
      </div>
      <h3 class="empty-title">Nothing to see here</h3>
      <p class="empty-text">{emptyText}</p>
      <a href="/write" class="btn-primary empty-cta">
        Start writing
      </a>
    </div>
  )
}

<style>
  .story-list {
    display: flex;
    flex-direction: column;
    gap: 16px;
  }

  .story-card {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    padding: 32px;
    background: var(--bg-surface-card);
    border: 1px solid var(--color-hairline);
    border-radius: 12px;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .story-card:hover {
    border-color: #6c6a64;
  }

  .story-content {
    display: flex;
    flex-direction: column;
    gap: 12px;
    max-width: 75%;
  }

  .story-title {
    font-family: 'StyreneB', 'Inter', sans-serif;
    font-size: 18px;
    font-weight: 500;
    margin: 0;
    line-height: 1.4;
    letter-spacing: 0;
  }

  .story-title a {
    color: var(--color-ink);
    text-decoration: none;
    transition: color 0.2s ease;
  }

  .story-title a:hover {
    color: #6c6a64;
  }

  .story-excerpt {
    font-family: 'StyreneB', 'Inter', sans-serif;
    font-size: 16px;
    font-weight: 400;
    line-height: 1.55;
    color: #3d3d3a;
    margin: 0;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .story-meta {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-top: 8px;
    font-family: 'StyreneB', 'Inter', sans-serif;
    font-size: 13px;
    font-weight: 500;
    color: #6c6a64;
  }

  .meta-item.badge {
    background: var(--bg-canvas);
    padding: 4px 12px;
    border-radius: 9999px;
    font-weight: 500;
    color: var(--color-ink);
    text-transform: uppercase;
    font-size: 12px;
    letter-spacing: 1.5px;
  }

  .story-actions {
    display: flex;
    align-items: center;
    gap: 8px;
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .story-card:hover .story-actions {
    opacity: 1;
  }

  .action-btn {
    display: flex;
    align-items: center;
    justify-content: center;
    background: transparent;
    border: none;
    color: #6c6a64;
    cursor: pointer;
    width: 36px;
    height: 36px;
    border-radius: 50%;
    transition: all 0.2s ease;
  }

  .action-btn:hover {
    color: var(--color-ink);
    background: var(--bg-surface-soft);
  }

  .action-btn.delete:hover {
    color: #c64545;
    background: var(--bg-canvas);
  }

  .story-options {
    background: none;
    border: none;
    color: #6c6a64;
    cursor: pointer;
    width: 36px;
    height: 36px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 50%;
    transition: all 0.2s ease;
  }

  .story-options:hover {
    color: var(--color-ink);
    background: var(--bg-surface-soft);
  }

  .stories-empty-state {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: 64px 32px;
    background: var(--bg-canvas);
    border: 1px solid var(--color-hairline);
    border-radius: 12px;
    margin-top: 32px;
  }

  .empty-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 64px;
    height: 64px;
    background: transparent;
    color: var(--color-ink);
    margin-bottom: 24px;
  }

  .empty-title {
    font-family: var(--font-serif);
    font-size: 28px;
    font-weight: 400;
    color: var(--color-ink);
    margin: 0 0 12px 0;
    letter-spacing: -0.3px;
    line-height: 1.2;
  }

  .empty-text {
    font-family: 'StyreneB', 'Inter', sans-serif;
    font-size: 16px;
    font-weight: 400;
    line-height: 1.55;
    color: #3d3d3a;
    margin: 0 0 32px 0;
    max-width: 400px;
  }

  .btn-primary.empty-cta {
    font-family: 'StyreneB', 'Inter', sans-serif;
    font-size: 14px;
    font-weight: 500;
    color: #ffffff;
    background: var(--color-primary);
    border-radius: 8px;
    padding: 0 20px;
    height: 40px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    text-decoration: none;
    transition: background 0.2s;
  }

  .btn-primary.empty-cta:hover {
    background: #252320;
  }
</style>
